<script type="text/javascript">
  <% if signed_in? %>
    window.currentUser = <%=
      render(
        "api/users/user.json.jbuilder",
        user: current_user
      ).html_safe
    %>
  <% end %>

  window.images = {
    wordLogo: "<%= image_url("medium-word-dark100") %>",
    picLogo: "<%= image_url("medium-dark100") %>"
  }
</script>

<main id="root">This is where it all happens</main>
